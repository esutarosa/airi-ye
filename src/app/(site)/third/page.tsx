import type { FC } from "react";
import Layout from "@/components/layouts/layout";

const HomePage: FC = () => {
  return (
    <Layout>
      <div>Third page</div>
    </Layout>
  );
};

export default HomePage;
